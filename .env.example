SECRET=
PUBLIC_URL=http://localhost:8055
ADMIN_EMAIL=
ADMIN_PASSWORD=

WEBSOCKETS_ENABLED="true"

# PostgreSQL Configuration
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

DB_CLIENT="pg"
DB_HOST='database'
DB_PORT='5432'
DB_DATABASE=
DB_USER=
DB_PASSWORD=

CACHE_ENABLED='false'
CACHE_STORE='redis'
CACHE_REDIS='redis://cache:6379'

# STORAGE_LOCAL_ROOT="./uploads"
LOG_LEVEL=debug

STORAGE_LOCATIONS='r2'
R2_ENDPOINT='https://.r2.cloudflarestorage.com'
R2_ACCESS_KEY=
R2_SECRET_KEY=
R2_BUCKET=

# Access Token TTL (default: 15m)
ACCESS_TOKEN_TTL=1440m

# Refresh Token TTL (default: 7d)
REFRESH_TOKEN_TTL=30d

# Static Token TTL (for server-to-server)
# STATIC_TOKEN_TTL=1y

# Optional: Disable token expiration entirely (not recommended for security)
# ACCESS_TOKEN_TTL=0

